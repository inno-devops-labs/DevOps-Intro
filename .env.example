# Copy to .env (gitignored). Never commit real credentials.
# Grafana — initial admin user. First boot creates the user; later boots ignore these.
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-before-first-boot
