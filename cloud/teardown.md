# Teardown
- HF Space: huggingface.co -> Space Settings -> Delete this Space (free tier, so leaving it up is also fine).
- ghcr image: github.com -> Packages -> quicknotes -> Package settings -> Delete package (kept for grading).
- Cloudflare quick tunnel: Ctrl+C on cloudflared - the URL dies with the process, nothing persists.
