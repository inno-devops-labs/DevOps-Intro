; Local inventory used by ansible-pull inside the VM:
; the VM reconciles itself, so the only target is localhost.
; The group name matches `hosts: lab5` in playbook.yaml.

[lab5]
localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3
