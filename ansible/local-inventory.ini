# Used by ansible-pull INSIDE the VM — the machine reconciles itself, so the
# connection is local (no SSH back into ourselves).
[quicknotes_vm]
localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3
