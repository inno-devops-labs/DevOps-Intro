# Local inventory used by ansible-pull INSIDE the VM (the VM reconciles itself).
# No SSH: the target is the loopback, reached via the local connection plugin.
[quicknotes_vm]
127.0.0.1 ansible_connection=local
