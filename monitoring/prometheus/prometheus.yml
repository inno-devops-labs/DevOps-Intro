global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: quicknotes
    metrics_path: /metrics
    static_configs:
      # Compose DNS resolves the service name; QuickNotes listens on 8080.
      - targets: ["quicknotes:8080"]
