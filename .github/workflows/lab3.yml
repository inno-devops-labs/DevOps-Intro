name: Lab3 Workflow

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Print hello
        run: echo "Hello from CI!"

      - name: Print date
        run: date