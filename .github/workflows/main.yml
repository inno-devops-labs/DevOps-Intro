name: Basic CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Print basic info
        run: |
          echo "Hello CI"
          uname -a
          date
